import React from 'react';

const BlocksHint: React.FC = () => (
  <p className="text-gray-500 mb-6">Type /list, /table, /chart, /image, /latex, or chart tokens like /bar, /line then press Enter</p>
);
 
export default BlocksHint; 